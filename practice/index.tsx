import React from "react";
import { StyleSheet } from "react-native";
// Class components
// React Memo
// event bubbling & event capturing
// named export, named imports - invariant voilation
// useRef in React, Global state management, State props,
// Don't put numbers, strings on left side of &&

// Responsive design
//

const RNBSectionList = () => {
  return <></>;
};

const styles = StyleSheet.create({
  section: {
    padding: 8,
    backgroundColor: "lightgray",
  },
  item: {
    margin: 8,
    fontSize: 18,
  },
});

export default RNBSectionList;
